{% extends 'base.html.twig' %}

{% block title %}National{% endblock %}

{% block body %}
    <h1>National</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ national.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ national.name }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ national.country }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_national_index') }}">back to list</a>

    <a href="{{ path('app_national_edit', {'id': national.id}) }}">edit</a>

    {{ include('national/_delete_form.html.twig') }}
{% endblock %}
